export const myStoreLocators = {
    screenTitle:
        '//*[contains(@name, "MyStore") or contains(@label, "MyStore") or contains(@value, "MyStore") or contains(@name, "My Store") or contains(@label, "My Store") or contains(@value, "My Store")]',
} as const;